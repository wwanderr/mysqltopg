<?xml version="1.0" encoding="UTF-8" ?>
<!--
    自动转换说明：
    - 原文件: ScenarioDataMapper.xml
    - 转换时间: 2026-01-19 11:04:29
    - MySQL → PostgreSQL 16.x
    
    注意事项：
    1. ON CONFLICT子句可能需要手动指定冲突列
    2. LIKE已保留，如需不区分大小写请改为ILIKE
    3. 建议人工review后再部署到生产环境
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/dtd">
<mapper namespace="com.dbapp.extension.xdr.threatMonitor.mapper.ScenarioDataMapper">

    <insert id="insertOrUpdate" parameterType="com.dbapp.extension.xdr.threatMonitor.entity.ScenarioData" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_event_scenario_data
        (result,log_session_id,incident_id,risk_id,scenario_id,update_time,focus_ip,target_ip,conclusion)
        values
        (#{result,jdbcType=VARCHAR},#{logSessionId,jdbcType=VARCHAR},#{incidentId,jdbcType=INTEGER},#{riskId,jdbcType=INTEGER},#{scenarioId,jdbcType=INTEGER},
        CAST(#{updateTime} AS timestamp),#{focusIp,jdbcType=VARCHAR},#{targetIp,jdbcType=VARCHAR},#{conclusion,jdbcType=VARCHAR})
        ON CONFLICT DO UPDATE SET update_time = EXCLUDED.update_time
    </insert>

</mapper>